const express = require('express');
const router = express.Router();

// TODO: implementar por integrantes 3 y 4
router.get('/mi-portal', (req, res) => res.status(501).json({ message: 'Not implemented' }));

module.exports = router;
